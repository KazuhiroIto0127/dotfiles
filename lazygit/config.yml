gui:
  language: 'en'
  showIcons: true
  theme:
    selectedLineBgColor:
      - underline
    selectedRangeBgColor:
      - underline
  sidePanelWidth: 0.2
git:
  autofetch: true
  merging:
    args: ""
    manualcommit: false
  paging:
    colorArg: always
    pager: delta --paging=never --24-bit-color=never
  refresher:
    refreshInterval: 3
os:
  editCommand: 'nvim'
